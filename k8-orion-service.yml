apiVersion: v1
kind: Service
metadata:
  name: orion-service
  namespace: default
spec:
  selector:
    app: orion-service
  type: ClusterIP
  ports:
    - name: tcp-port
      port: 8080
      targetPort: 8080
      protocol: TCP
